<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.training.repository.OrderRepository">

	<insert id="orderUserSave" useGeneratedKeys="true"
		keyProperty="id">
		insert into ORDERS (number, name, address, date)
		values(
		concat(",${phone},"),
		concat(",${fullName},"),
		concat(",${fullAddress},"),
		concat(",${dateNow},")
		)
	</insert>
	<insert id="orderProductSave">
		insert into ORDER_ITEMS (order_id, name, price, quantity)
		values(
		${ordreId},
		concat(",${cartItem.productName},"),
		${cartItem.productPrice},
		${cartItem.quantity}
		)
	</insert>
</mapper>
